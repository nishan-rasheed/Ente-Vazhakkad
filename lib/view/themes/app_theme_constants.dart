import 'package:flutter/material.dart';

class AppTheme {
     static const backgroundColor = Colors.red;
     static const primaryColor =  Colors.white;
     static const appBarColor =  Colors.red;

     static const newColor = Color(0xff783783);
}





/// flutter pub run easy_localization:generate --source-dir ./assets/trans --to generate lang files